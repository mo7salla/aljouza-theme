{#
| Variable         | Type    | Description                     |
|------------------|---------|---------------------------------|
| cart             | object  |                                 |
| cart.items_count | int     |                                 |
| cart.total       | string  | Formatted total ex: "١٠٠ ر.س"   |
#}
{% set important_links = theme.settings.get('important_links') %}
<header class="store-header">
  
  {# Top Nav (مخصص للروابط الإضافية، اللغات، والفروع فقط) #}
  <div class="top-navbar">
    <div class="container flex justify-between">
      <div class="flex-1 flex items-center gap-2">
          
          {% if important_links %}
            <salla-menu source="footer" topnav></salla-menu>
          {% endif %}

          <div class="header-buttons">
              {% if store.settings.is_multilingual or store.settings.currencies_enabled %}
                  <salla-localization-modal show-trigger></salla-localization-modal>
              {% endif %}
          </div>

          {% if store.scope %}
              <button class="btn--rounded-gray"
                      onclick="salla.event.dispatch('scopes::open', {'mode': 'default'})">
                  <i class="sicon-location rtl:ml-1 ltr:mr-1"></i> {{ store.scope.name }}
              </button>
          {% endif %}
      </div>
    </div>
  </div>

  {# Main Nav (توزيع العناصر: يمين - وسط - يسار) #}
  <div id="mainnav" class="main-nav-container shadow-default bg-white flex items-center">
      <div class="inner bg-inherit w-full">
          <div class="container">
              <div class="flex items-center justify-between relative w-full">
                  
                  {# 1. اليمين: الشعار وزر القائمة في الجوال #}
                  <div class="flex items-center lg:w-1/3">
                      <a class="lg:hidden mburger mburger--collapse leading-none rtl:ml-4 ltr:mr-4" href="#mobile-menu" aria-label="Open-menu">
                        <i class="sicon-menu text-primary text-2xl"></i>
                      </a>
                      <a class="navbar-brand" href="{{ store.url }}">
                          <img fetchpriority="high" width="100%" height="100%" loading="eager" src="{{ store.logo|cdn(175) }}" alt="{{ store.name }} logo">
                          {% if is_page('index') %}
                            <h1 class="sr-only">{{ store.name }}</h1>
                          {% else %}
                            <h2 class="sr-only">{{ store.name }}</h2>
                          {% endif %}
                      </a>
                  </div>

                  {# 2. المنتصف: القائمة الرئيسية للروابط #}
                  <div class="hidden lg:flex flex-1 justify-center">
                      <custom-main-menu></custom-main-menu>
                  </div>

                  {# 3. اليسار: البحث، المستخدم، والسلة #}
                  <div class="flex items-center justify-end lg:w-1/3 gap-3 my-2.5">
                      
                      {# البحث #}
                      <div class="header-search hidden sm:block">
                        <salla-search inline oval height="36"></salla-search>
                      </div>

                      {# المستخدم #}
                      <salla-user-menu avatar-only show-header show-trigger></salla-user-menu>
                      
                      {# السلة #}
                      <salla-cart-summary>
                        <i slot="icon" class="header-btn__icon icon sicon-shopping-bag m-0"></i>
                      </salla-cart-summary>
                      
                  </div>

              </div>
          </div>
      </div>
  </div>
</header>